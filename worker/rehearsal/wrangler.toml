name = "rehearsal-writer"
main = "index.js"
compatibility_date = "2026-09-01"

# Set secrets with:  npx wrangler secret put ANTHROPIC_API_KEY
# Set plain vars in the dashboard or here:
[vars]
MODEL = "claude-opus-5"
ALLOWED_ORIGIN = "https://tsjenn.github.io"
# ALLOWED_HASHES = "sha256hex1,sha256hex2"   — never commit the codes themselves
